# Prefijo de la API. Vacío = mismo origen (en desarrollo lo resuelve el proxy de Vite hacia 127.0.0.1:3000).
# Ninguna variable VITE_* puede contener un secreto: todas terminan en el bundle público (AGENTS.md, regla 9).
VITE_API_URL=
